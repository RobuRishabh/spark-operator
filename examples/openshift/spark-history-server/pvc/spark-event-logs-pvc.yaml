apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: spark-event-logs-pvc
  namespace: spark-test
spec:
  accessModes:
    - ReadWriteOnce  # EBS limitation - only one pod can mount
  resources:
    requests:
      storage: 20Gi
  storageClassName: gp3-csi  # ROSA default StorageClass
